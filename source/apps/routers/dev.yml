dev-phpinfo:
    active: true
    pattern: "/phpinfo"
    methods:
        - "GET"
    callback: "phpinfo"
dev-input:
    active: true
    pattern: "/input"
    methods:
        - "ANY"
    controller: "Dev"
    action: "input"
dev-hash:
    active: true
    pattern: "/hash"
    methods:
        - "GET"
    controller: "Dev"
    action: "hash"
dev-env:
    active: false
    pattern: "/env"
    methods:
        - "ANY"
    controller: "Dev"
    action: "env"
