slim:
    !imports:
        - './slim.yml'

app:
    timezone: 'Europe/Moscow'

    debug: true
    sqlLog: false

    salt: '!@#$%^&*()'

    database:
        !imports:
            - './database.yml'
