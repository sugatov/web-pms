slim:
    !imports:
        - './slim.yml'

app:
    timezone: 'Europe/Moscow'

    debug: true
    sqlLog: true

    salt: '!@#$%^&*()'

    database:
        !imports:
            - './database.yml'
