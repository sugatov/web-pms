slim:
    !imports:
        - './slim.yml'

app:
    timezone: 'Europe/Moscow'

    debug: false
    sqlLog: false

    salt: '!@#$%^&*()'

    database:
        !imports:
            - './database.yml'
