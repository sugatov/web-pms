dev-phpinfo:
    active: true
    pattern: "/phpinfo"
    methods:
        - "GET"
    callback: "phpinfo"
dev-input:
    active: true
    pattern: "/input"
    methods:
        - "ANY"
    controller: "dev"
    action: "input"
dev-hash:
    active: true
    pattern: "/hash"
    methods:
        - "ANY"
    controller: "dev"
    action: "hash"
dev-env:
    active: false
    pattern: "/env"
    methods:
        - "ANY"
    controller: "dev"
    action: "env"
